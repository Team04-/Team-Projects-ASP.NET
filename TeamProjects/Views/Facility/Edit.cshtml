@model TeamProjects.Models.timetable_facility

@{
    ViewBag.Title = "Edit @Model.Facility_Name";
}

<h2>Edit @Model.Facility_Name</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>timetable_facility</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Facility_ID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Facility_ID)
            @Html.ValidationMessageFor(model => model.Facility_ID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Facility_Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Facility_Name)
            @Html.ValidationMessageFor(model => model.Facility_Name)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
